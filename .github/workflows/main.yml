name: Python CI/CD

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install pytest
        run: pip install pytest

      - name: Run tests
        run: pytest

      - name: Deploy step
        run: echo "Deploying application..."
